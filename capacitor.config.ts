import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'experiment_2_angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
